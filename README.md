# sizable_bucket_array
Container which looks like to std::deque, but a little bit different

## Dependencies
To compile sizable_bucket_array project, you need the following packages :
- googletest

```sh
apt-get install googletest
```